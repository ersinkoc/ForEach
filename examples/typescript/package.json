{
  "name": "foreach-typescript-examples",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript examples for @oxog/foreach",
  "main": "dist/advanced-types.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/advanced-types.js",
    "dev": "ts-node advanced-types.ts"
  },
  "dependencies": {
    "@oxog/foreach": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}